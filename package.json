{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "It's a simple weather app which fetchs data from an api and display the live weather to the user with get request and api concepts",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
